import React from 'react';

export default function reducer(state = {
    animeName: '',
    slug: '',
    animeID: 0,
    malID: 0
}, action) {
    switch(action.type) {
        default: return state
    }
}